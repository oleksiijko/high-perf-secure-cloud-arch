apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-svc
  namespace: microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: content-svc
  template:
    metadata:
      labels:
        app: content-svc
        role: backend
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: content-svc
          image: ghcr.io/example/content-svc:1.0
          ports:
            - containerPort: 3000
          env:
            - name: CERT_DIR
              value: /certs
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          volumeMounts:
            - name: tls
              mountPath: /certs
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: tls
          secret:
            secretName: tls-secret
---
apiVersion: v1
kind: Service
metadata:
  name: content-svc
  namespace: microservices
spec:
  selector:
    app: content-svc
  ports:
    - port: 80
      targetPort: 3000
